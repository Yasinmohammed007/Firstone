apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "samplechart.fullname" . }}-test-connection"
  labels:
    {{- include "samplechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: python-docker
  restartPolicy: Never
